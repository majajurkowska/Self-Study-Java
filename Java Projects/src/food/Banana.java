package food;

public class Banana
{
    private String color = "yellow";
    //private can be used in the same class

    int peels = 4; // here, we didn't specify public or private, so it has a default access modifier
    // it can be accessed from the same class and from the same package
}
